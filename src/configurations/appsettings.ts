export const AppSettings: AppSettings = {
    ConnectionStrings: {
        DefaultConnection:{
            server: "localhost",
            port: "3306",
            database: "wibase",
            user: "root",
            password: "root",
            dialect: 'mysql'
        }
    }
}